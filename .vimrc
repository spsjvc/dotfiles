" Show line numbers
set number

" Use spaces instead of tabs
set expandtab

" Change tab size to 4
set tabstop=4

" Turn on syntax highlighting
syntax on

" Change encoding to UTF-8
set encoding=utf-8
